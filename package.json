{
  "name": "conmath",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A resource-aware constructive mathematics book",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "npm run test && node scripts/check.mjs",
    "inventory": "node scripts/check.mjs --json"
  },
  "engines": { "node": ">=22" }
}
