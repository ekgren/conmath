{
  "name": "conmath",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/serve.mjs",
    "test": "node --test",
    "check:content": "node scripts/check-content.mjs",
    "check": "npm run test && npm run check:content"
  },
  "engines": {
    "node": ">=20"
  }
}
