# Changelog

## Unreleased

- Replace the opening instruction machine with a seven-line Python successor program, explicit reads and writes, locally bundled WebAssembly execution, and independently checked access records.

- Add the first reading draft, five concept pages, a finite row machine with execution-certificate checking, and a static book layout with runnable source and evidence export.
- Extend the harness with deterministic publishing, source parity, complete small-machine boundary tests, browser acceptance, and a reproducible diagnostics runbook.

- Preserve the previous executable book and all manuscript sources in commit `df6b32b` before removing it from the active tree.
- Restart Conmath with an explicit foundation charter, multi-page book structure, editorial direction, recorded design interview, and first-chapter plan.
- Add a dependency-free repository harness for documentation links, content structure, source boundaries, and machine-readable readiness reporting.

Earlier releases and implementation details remain in Git history.
